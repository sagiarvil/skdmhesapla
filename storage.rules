rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /sealed_packages/{allPaths=**} {
      allow read: if false; // imzalı URL ile
      allow write: if false; // yalnızca Functions
    }
  }
}
