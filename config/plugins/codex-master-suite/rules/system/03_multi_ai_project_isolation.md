# MULTI-AI WORKSPACE ROUTING & ISOLATION PROTOCOL

1. **Isolated Working Environments:** Gemini and Claude instances operate in dedicated, isolated project trees registered in `ai_projects_map.json`.
2. **Cross-AI Invariant:** Both environments share and are strictly governed by this identical, unified set of rules under `rules/`.
