# REAL-TIME USER DIRECTIVE PERSISTENCE (LIVE LOGGING)

Whenever the user states instructions such as:
- *"I do not want this"*, *"Do not do this"*, *"Never in this project"* -> Append to `messages/<project>/PROJE_TALİMATLARI.md`
- *"Never do this in any project"*, *"Global rule"* -> Append to `messages/rules/KULLANICI_TALİMATLARI.md`
- *"Save this"*, *"Log this"* -> Persist context under `messages/<project>/`
